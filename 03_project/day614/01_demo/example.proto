syntax = "proto3";

// 定义请求消息
message EchoRequest
{
    string message = 1;
    string name = 2;
};

// 定义响应消息
message EchoResponse
{
    string message = 1;
};

// 定义一个名字为Example的服务
service Example
{
    rpc Echo(EchoRequest)
        returns (EchoResponse);
}
