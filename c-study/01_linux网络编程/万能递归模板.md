```c
// 1. 核心逻辑：只管干活，不碰内存申请
int do_the_work(char *target) {
    // 这里写递归逻辑...
    // do_the_work(sub_target);
    return success_or_fail;
}

// 2. 外部接口：负责内存，包装结果
char* api_function(char *target) {
    char *result = malloc(1024); // 只申请一次
    if (do_the_work(target) == 0) {
        strcpy(result, "操作成功");
    } else {
        strcpy(result, "操作失败");
    }
    return result; 
}

//在执行函数中释放result
```